rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY PROMISCUOUS RULES For Data Seeding
    // effectively "Anyone signed in can change anything"
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    
  }
}
